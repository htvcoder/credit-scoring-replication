# Data Card: TH02

## Nguồn và phiên bản

- Dataset: public credit scoring data from *Credit Scoring and Its Applications*, Thomas, Edelman and Crook, SIAM, 2002.
- Registry entry: `data/datasets.yaml`, dataset id `th02`.
- Raw data: `data/raw/th02/public.xls`.
- Dictionary: `data/raw/th02/publicdict.xls`.
- Processed conversion artifact: `data/processed/th02/th02.csv`.
- Raw format: Microsoft Excel BIFF2 legacy workbook; this is a tool compatibility issue, not evidence of a corrupted dataset.

## Schema

| Cột | Vai trò | Kiểu kiểm tra | Ghi chú |
|---|---|---|---|
| `DOB` | Input | Numeric | Year of birth coding per dictionary. |
| `NKID` | Input | Numeric | Number of children. |
| `DEP` | Input | Numeric | Number of dependents. |
| `PHON` | Input | Numeric | Phone indicator. |
| `SINC` | Input | Numeric | Salary income. |
| `AES` | Input | Categorical | Employment status/category from dictionary. |
| `DAINC` | Input | Numeric | Applicant income-related variable. |
| `RES` | Input | Categorical | Residential status/category from dictionary. |
| `DHVAL` | Input | Numeric | House value-related variable. |
| `DMORT` | Input | Numeric | Mortgage-related variable. |
| `DOUTM` | Input | Numeric | Outstanding mortgage-related variable. |
| `DOUTL` | Input | Numeric | Outstanding loan-related variable. |
| `DOUTHP` | Input | Numeric | Outstanding hire purchase-related variable. |
| `DOUTCC` | Input | Numeric | Outstanding credit card-related variable. |
| `BAD` | Target | Numeric/binary | `1 = bad/default`, `0 = good/non-default`. |

## Verification Summary

| File | SHA-256 | Shape | BAD=0 | BAD=1 | Default rate | Missing values | Duplicate rows | Core usable |
|---|---|---:|---:|---:|---:|---:|---:|---|
| `data/raw/th02/public.xls` | `F9A3707AE845091C5DC3C5F8CCFD6B5438AD01D4AB0E70A3F25BD396B2014DA3` | 1,225 x 15 | 902 | 323 | 0.2636734694 | 0 | 23 | Yes after conversion/read step |
| `data/processed/th02/th02.csv` | `C9CB96394DDE17CF82C3ABC5D8FFE465DC1F68CE442D680E868A7B9224AD59B7` | 1,225 x 15 | 902 | 323 | 0.2636734694 | 0 | 23 | Yes |

## Conversion Method

The reusable command is:

```powershell
python scripts/convert_th02.py --input data/raw/th02/public.xls --output data/processed/th02/th02.csv
```

The successful conversion used `xlrd` and did not modify `data/raw/th02/public.xls`. The script validates shape, schema, target domain, class counts and absence of generated index columns before writing output.

## Dictionary Notes

`publicdict.xls` is readable with `xlrd`. It contains a variable dictionary in `Sheet1`; `Sheet2` and `Sheet3` are empty. The current SHA-256 of the dictionary is `639DBECD784A62D4BFEF16154FEB71BDE71E11D7DEE8626E23CAA76B31B0A62F`.

## License and Conditions

License remains `Chưa xác định`. The source is book supplementary material; redistribution conditions should be checked before publishing raw or converted data.

## Preprocessing Status

The CSV conversion preserves values and schema for tooling compatibility. No imputation, WOE, VIF, class balancing, split creation, or duplicate removal has been applied. Duplicate rows are retained to preserve the 1,225 observations used by the paper.

Numeric columns are `DOB`, `NKID`, `DEP`, `PHON`, `SINC`, `DAINC`, `DHVAL`, `DMORT`, `DOUTM`, `DOUTL`, `DOUTHP`, `DOUTCC`. Categorical columns are `AES`, `RES`. There are no identifier or ignored columns.
